server:
  port: 8081

spring:
  application:
    name: observability-demo

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      show-details: always
  metrics:
    tags:
      application: observability-demo

logging:
  level:
    root: INFO
    edu.eci.arsw.observability: DEBUG
  file:
    name: logs/observability-demo.log
  pattern:
    file: "%d{yyyy-MM-dd'T'HH:mm:ss.SSS} %-5level [%thread] %logger{36} - %msg%n"
